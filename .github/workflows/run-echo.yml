name: run-echo

on: workflow_call

jobs:
    run-echo-job:
        name: run echo.
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v3
            - name: echo!!
              run: echo "call workflow!!"
